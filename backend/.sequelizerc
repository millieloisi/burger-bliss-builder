const path = require('path');

// Paths are resolved relative to this .sequelizerc file (inside backend/). Using __dirname avoids
// accidentally adding an extra 'backend' when running npm from inside the backend folder.
module.exports = {
  'config': path.resolve(__dirname, 'config', 'config.js'),
  'models-path': path.resolve(__dirname, 'src', 'models'),
  'migrations-path': path.resolve(__dirname, 'migrations'),
  'seeders-path': path.resolve(__dirname, 'seeders')
};
